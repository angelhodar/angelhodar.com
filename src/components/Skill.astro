---
import { Icon } from "astro-icon/components";

const { name, icon } = Astro.props;
---

<div class="flex flex-col space-y-3 items-center">
    <Icon name={icon} class:list={["w-12 h-12", Astro.props.class]} />
    <p >{name}</p>
</div>
