---
import { Icon } from "astro-icon/components";

const { name, icon } = Astro.props;
---

<div class="flex flex-col items-center gap-2">
    <Icon name={icon} class:list={["w-10 h-10", Astro.props.class]} />
    <span class="text-xs text-neutral-600 dark:text-neutral-400">{name}</span>
</div>
